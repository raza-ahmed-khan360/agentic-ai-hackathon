# Frontend Environment Configuration for Docusaurus
# Create a .env.local file (NOT committed) with these variables for local development

# For local development: point to localhost backend
REACT_APP_BACKEND_URL=http://127.0.0.1:8000

# For production (Vercel): backend URL will be resolved via window.location.hostname check
# Or set explicitly:
# REACT_APP_BACKEND_URL=https://your-backend-vercel-url.vercel.app/api
